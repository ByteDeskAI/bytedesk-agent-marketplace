# Catalog (generated)

`index.json` and the OASF discovery projection are generated by
`scripts/build_catalog.py`; nothing in this directory is hand-edited, and
`index.json` itself is gitignored. Run `make verify` to build it locally.
