# Agents

`Agent` and `SpecializedAgent` Agent Spec 26.1.2 source manifests go here, one
per directory. Nothing here may declare required MCP servers, tool/resource
grants, provider credentials, tenant identifiers, or workload bindings — CI
rejects those with an actionable field path.

Empty until the first example lands (see the repository `README.md` status
checklist).
